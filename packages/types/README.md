# @erp/types

Placeholder package — reserved for TypeScript types shared between `apps/web`
and a future `apps/api` backend (e.g. domain models like `Order`, `Customer`,
`Invoice`). No types have been migrated here yet; `apps/web` still owns all
of its types directly under `apps/web/src/lib/types.ts`.
