# @erp/ui

Placeholder package — reserved for shared UI components once a second app
(e.g. `apps/api`'s admin tooling, or a future mobile client) needs to share
components with `apps/web`. No components have been migrated here yet;
`apps/web` still owns all of its UI directly under `apps/web/src/components`.
